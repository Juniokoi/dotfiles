local EDITOR="nvim"

# Shortcuts
alias b="bash"
alias e="ranger ."
alias {zz,s,f}="zsh"
alias {c,cl,cle,clea}="clear"

# Configuration
alias bc="$EDITOR $HOME/.bashrc"                    # Bash 
alias ac="$EDITOR $HOME/.config/awesome/rc.lua"     # Awesome 
alias kc="$EDITOR $HOME/.config/kitty/kitty.conf"   # Kitty


# GNU
alias cp="cp -i" # Confirm before overwriting
alias mv="mv -i" #      --/--     moving/renaming
alias rm="rm -i" #      --/--     deleting

# ZSH 
alias {zc,cv}="$EDITOR $HOME/.config/zsh/.zshrc"    # Main file
alias za="$EDITOR $HOME/.config/zsh/zsh-aliases"    # Alises
alias ze="$EDITOR $HOME/.config/zsh/zsh-exports"    # Exports
alias zf="$EDITOR $HOME/.config/zsh/zsh-functions"  # Functions

# System commands
alias logoff="pkill -KILL -u $USER"
alias sayonara="shutdown -h now"
alias supersudo="sudo --preserve-env=PATH env"

# Tools and applications
# 
## exa & bat & zoxide
alias ls="exa --icons --color=always --color-scale --sort type "
alias la="ls -@lah --git"
alias lt="ls -T"
alias lta="ls -Ta"
alias cat="bat --style full --theme Dracula"
alias cd=z

## Terminals
#
### Kitty
alias kc="$EDITOR $HOME/.config/kitty/kitty.conf"
### Wezterm
alias wc="$EDITOR $HOME/.config/wezterm/wezterm.lua"

# GIT
alias gs="git-st"
alias gc="git commit -m"
alias gl="git lg"
alias glo="git log --oneline"
alias ga="git add"
alias lg="lazygit"

## Emacs
alias emacs="emacsclient -c -a 'emacs'"

## Docker
alias di="docker image"
alias dv="docker volume"
alias dc="docker container"
